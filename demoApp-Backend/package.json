{
  "name": "demoapp-server",
  "version": "1.0.0",
  "description": "server side code for React-Native Project",
  "main": "server.js",
  "dependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-node": "babel-node --presets=es2015",
    "start": "nodemon --exec npm run babel-node -- ./server.js",
    "build": "babel src -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.17.1"
  }
}
